// Generated by CoffeeScript 1.8.0
(function() {
  define('BE.Progress', ['$', 'BE.Views.Progress', 'BE.Logic'], function($, View, logic) {
    var progress, track;
    progress = [
      new View({
        model: logic
      }), new View({
        model: logic
      })
    ];
    progress[0].selections(['concern', 'skintype', 'finish', 'coverage', 'undertone']);
    progress[0].pages([1, 2, 3, 4, 5, 6, 9]);
    progress[1].selections(['family', 'undertone']);
    progress[1].pages([1, 7, 8]);
    track = function() {
      switch (logic.get('track')) {
        case 1:
          progress[0].show();
          progress[1].hide();
          $('.sidebar-0').show();
          return $('.sidebar-1').hide();

          /*
          				$('.menu-toggle a').toggleClass('active')
          				$('.sidebar').toggleClass('open')
           */
        case 2:
          progress[1].show();
          progress[0].hide();
          $('.sidebar-1').show();
          return $('.sidebar-0').hide();
      }
    };

    /*
    	close = () ->
    		$('.menu-toggle a').toggleClass('active')
    		$('.sidebar').toggleClass('open')
    	 *setTimeout(run, 4000)
     */
    logic.on('change:track', track);
    $(function() {
      progress[0].create($('.progress-0'));
      return progress[1].create($('.progress-1'));
    });
    return progress;
  });

}).call(this);
